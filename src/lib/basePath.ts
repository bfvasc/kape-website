// Keep this in sync with `basePath` in next.config.mjs.
// Next.js only auto-prefixes basePath for <Link>/router navigation, not for
// public-folder asset URLs, so we prefix those explicitly here.
export const basePath = "/kape-website";
